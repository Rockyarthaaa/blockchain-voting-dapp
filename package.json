{
  "name": "voting-dapp",
  "version": "1.0.0",
  "description": "Decentralized Voting System on BNPI Network",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:bnpi": "hardhat run scripts/deploy.js --network bnpi",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "keywords": ["blockchain", "voting", "ethereum"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0"
  }
}